# Counter with If Statements

This mini project introduces conditional statements (`if / else`).

Increment logic:
- If counter is less than 10 → increase
- Else → show message

Decrement logic:
- If counter is greater than 0 → decrease
- Else → show message
