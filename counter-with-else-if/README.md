#Counter with (if / else if / else)
#just a little use of logical operators('&&' means 'And')

logic concept:

if (condition1 is true) {
   // do something1
} else if (condition2) {
   // do something else
} else {
   // fallback (do the final thing)
}
